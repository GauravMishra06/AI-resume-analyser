{
  "name": "ai-resume-analyzer-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && rm -rf ../dist && mv dist ../dist",
    "build:server": "cd server && npm install && npm run build"
  }
}
